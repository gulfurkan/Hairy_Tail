import 'package:flutter/material.dart';

class DrawerHeaderItem extends StatelessWidget {
  const DrawerHeaderItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Drawer Header"),
    );
  }
}
